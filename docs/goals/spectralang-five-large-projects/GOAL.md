# Goal: Five Large Spectra Projects

Implement `docs/goals/spectralang-five-large-projects/PLAN.md`.

Add five connected complete applications under `examples/complete`, each with
more than 25 real `.spectra` files, covering core language processing, data
platform operations, ML training, API service development, and governed agent
operations. Preserve existing work, validate JIT/AOT behavior, and do not
commit or push.
