# Goal: Spectra ML/AI Workbench

Implement the application described in
`docs/goals/spectralang-ml-ai-workbench/PLAN.md`.

The project must contain more than 25 real `.spectra` files, use the public
tensor/ML/RAG/serving/experiment standard-library surfaces in one connected
workflow, include an independent verification project, and finish with JIT/AOT
runtime evidence. Preserve unrelated worktree changes. Do not commit or push.
