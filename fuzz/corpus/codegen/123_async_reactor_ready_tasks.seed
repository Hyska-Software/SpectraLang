module async_reactor_ready_tasks

async func ready_value(value: int) returns int {
    return value
}

async func combine() returns int {
    let left  = await ready_value(20)
    let right = await ready_value(22)
    return left + right
}

func main() returns int {
    let task = combine()
    return 0
}
