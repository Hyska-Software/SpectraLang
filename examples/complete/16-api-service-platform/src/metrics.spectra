module metrics

import std.convert as convert

public func api_platform_metrics(routes: int, page: int) returns string {
    return "metrics:routes=" +
        convert.int_to_string(routes) +
        ";page=" +
        convert.int_to_string(page) +
        ";requests=5"
}
