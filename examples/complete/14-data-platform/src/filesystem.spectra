module filesystem

import std.fs as fs
import std.result as result
import paths

public func data_write_report(root: string, report: string) returns int {
    let written = fs.fs_write(paths.data_report_path(root), report)
    if result.is_err(written) {
        return 1
    }
    return 0
}
