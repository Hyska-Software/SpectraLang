module raw_json

import std.fs as fs
import std.result as result

public func data_read_json(path: string) returns string {
    let loaded = fs.fs_read(path)
    if result.is_err(loaded) {
        return ""
    }
    return result.result_unwrap(loaded)
}
