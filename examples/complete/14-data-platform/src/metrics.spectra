module metrics

import std.convert as convert

public func data_metrics(amount: int, active: int, batches: int) returns string {
    return "metrics:amount=" +
        convert.int_to_string(amount) +
        ";active=" +
        convert.int_to_string(active) +
        ";batches=" +
        convert.int_to_string(batches)
}
