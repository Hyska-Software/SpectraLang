// Test: Method call validation - should fail with error
// Expected: Semantic error - method not found (code: E017)
module method_error_test

func create_value() returns int {
    42
}

func main() returns int {
    let x = create_value()

    // Isso deve gerar erro: int não tem método 'nonexistent'
    x.nonexistent()
}
