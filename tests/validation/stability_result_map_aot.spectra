module stability_result_map_aot

import std.result as result

public func main() returns int {
    let mapped = result.result_map(Result::Ok(41), |value: int| value + 1)
    if result.result_unwrap(mapped) != 42 {
        return 1
    }
    return 0
}
