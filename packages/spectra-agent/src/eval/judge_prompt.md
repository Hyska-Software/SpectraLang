You grade one attempt of a Spectra agent evaluation case.

Case: {case}
Input: {input}
Rubric the case declares: {rubric}
Observed final output: {output}

Decide whether the observed output satisfies the rubric. Grade only what the
rubric asks for; ignore style, tone and length unless the rubric mentions them.
Answer with a single JSON object and nothing else:

{"pass": true, "reason": "<short reason>"}
