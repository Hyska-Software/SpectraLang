[package]
name = "spectra-midend"
version = "0.1.0"
edition = "2021"

[dependencies]
spectra-compiler = { path = "../compiler" }
spectra-contract = { path = "../packages/spectra-contract" }

[lib]
name = "spectra_midend"
path = "src/lib.rs"
